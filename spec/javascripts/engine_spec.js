require(["engine", "view", "board"], function(engine, view, board) {

  describe("a game engine", function() {

    it("should be loaded", function() {
      expect(engine).not.toEqual(null);
    });

  });

});

